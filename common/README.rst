============
Core
============

Core it is a Django app that contains utilities for the EDDAI project.

Quick start
-----------

1. Add "polls" to your INSTALLED_APPS setting like this::

    INSTALLED_APPS = [
        ...,
        "core",
    ]

2. Run ``python manage.py migrate`` to create the models.